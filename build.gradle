group 'com.cuponation'
version '1.0-SNAPSHOT'

apply plugin: 'java'
apply plugin: 'war'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    compile (
            'org.primefaces:primefaces:6.1'
    )
    providedCompile (
            'javax:javaee-api:7.0'
    )
}
